import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Create = () => {
  return <Text style={{ fontSize: 48 }}>Excursion Detail Screen</Text>;
};

const styles = StyleSheet.create({});

export default Create;
